# Copy this


 * `--private`
 * `--clone-path`
 * `--cache-control`

Maybe `--help`.

```
Usage: travis-uploader COMMAND [OPTIONS]

Commands
     upload: upload files to server

Options
        --path PATH                  path(s) to upload to a server
        --target-path TARGET_PATH    path to upload files to
        --clone-path CLONE_PATH      path to clone uploaded files to
        --root ROOT                  root directory for relative paths
        --private                    make artifacts non-public
        --cache-control CACHE_CONTROL_VALUE
                                     set custom cache control metadata value
    -h, --help                       help
